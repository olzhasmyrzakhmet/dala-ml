# TODO — отклонения от плана и незакрытые долги

Сюда попадает то, что решено осознанно, но расходится с CLAUDE.md / SPEC,
и то, что заявлено, но пока не выполнено. Без записи здесь такие вещи
превращаются в тихий обман.

## Отклонения от стека

### Контент в TSX, а не в MDX
CLAUDE.md: «контент в **MDX**». Фактически уроки — компоненты
`app/kurs/module-N/lesson-M/page.tsx`, а frontmatter вынесен в реестр
`lib/content.ts`.

Причина: при `output: 'export'` MDX-пайплайн добавлял сборочный слой ради
единственной выгоды — «текст отдельно от кода». Но каждый урок и так обязан
подключать свой интерактив и свой квиз, то есть без JSX всё равно не обойтись.
Реестр в `lib/content.ts` даёт то же, ради чего нужен frontmatter: единый
источник правды о списке уроков, их названиях и времени, с zod-валидацией и
тестами (`tests/content.test.ts`).

Цена решения: автор урока пишет TSX, а не Markdown. Для будущих
контрибьюторов порядок описан в README («Как добавить урок»).

### Нет `app/kurs/[module]/page.tsx`
SPEC §1 предполагает страницу модуля со списком уроков. Сейчас этот список
живёт прямо на карте курса `/kurs`, каждый модуль — раскрытая карточка.
Отдельная страница добавила бы лишний тап между «хочу учиться» и «читаю урок».
Если модулей станет больше восьми — вернуться к этому.

## Незакрытые долги

### Бюджет кадра не выполнен
SPEC §4 требует ≤16 мс на кадр на среднем Android. После переработки
(кэш неподвижных слоёв, остановка обучения после сходимости, пауза вне экрана,
метод Ньютона вместо градиентного спуска в логрегрессии) числа заметно упали,
но порог не взят стабильно.

Измерения (троттлинг CPU ×6, Pixel 5). Медиана кадра:

| Страница | До переработки | Локальная раздача | **Прод** |
|---|---|---|---|
| Главная | 93 мс | 17–117 мс | **50 мс** |
| Урок «Арық» | 109 мс | 17–83 мс | **33 мс** |
| Урок «Отар» | 203 мс | 133–350 мс | **33 мс** |
| Витрина восьми | — | 17–50 мс | **17 мс** |
| Базовая линия (страница без виджетов) | — | 17 мс | — |

Прод: `BASE_URL=https://dala-ml.vercel.app npm run e2e:perf`
Локально: `npm run build && npm run e2e:perf`

Два вывода:

1. Локальные числа для «Отара» (133–350 мс) были в основном конкуренцией за
   процессор с собственным dev-сервером и сборками, а не стоимостью виджета:
   на проде та же страница даёт 33 мс. Мерить перф на занятой машине нельзя.
2. Базовая линия при ×6 — 17 мс, это потолок vsync. Значит 33 мс — это 30 кадров
   в секунду на устройстве вшестеро медленнее машины разработчика, а 50 мс — 20.
   Заявленных SPEC §4 шестнадцати миллисекунд по-прежнему нет, но и «страница не
   отвечает» больше нет.

Что делать: снять числа на живом дешёвом телефоне (docs/CHECKLIST.md) и решить,
достаточно ли 30 fps для этих экранов. Худшая страница теперь — главная.

### Lighthouse не прогонялся
DoD §6: Performance ≥90, Accessibility ≥95. Цифр нет, заявлять нечего.

### Песочница Pyodide не начата
SPEC §7, PLAN 6.3. Это P1 — до неё очередь не дошла.

### Модули 4–8 без содержания
Каркас со статусом «дайындалуда». Осознанно: CLAUDE.md §11 требует три
идеальных модуля вместо восьми сырых. Интерактивы этих модулей доступны
на витрине `/interaktiv`.

### Плотность текста в уроках
SPEC §3: «абзац > 5 строк — разбей», «ни одного экрана подряд без визуала».
На 360px в четырёх уроках остаются абзацы по 6–9 строк и участки по 2–3 экрана
без картинки. Требует прохода по контенту.

### Клавиатура в «Таңба»
Тавро ставится тапом по холсту; с клавиатуры добавить точку нельзя. Виджет
работает и без этого (восемь стартовых тавро + ползунок), но полноценной
альтернативы для клавиатуры нет.
